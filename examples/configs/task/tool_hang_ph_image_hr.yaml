# @package task
defaults:
  - robomimic_base
  - _self_

env_name: "tool_hang"
obs_type: "image"
env_type: "ph"

# HuggingFace dataset configuration - auto-downloads from the repository
dataset_filename: "robomimic/tool_hang_hr/ph/image_abs.hdf5"

# Alternative: Use original repo with demo dataset (requires local processing)
# dataset_repo: "amandlek/robomimic"
# dataset_filename: "v1.5/tool_hang/ph/demo_v15.hdf5"

max_episode_steps: 700
render_obs_key: "sideview_image"

# Required keys
horizon: 10
act_dim: 10

shape_meta:
  action:
    shape:
      - 10
  obs:
    sideview_image:
      shape:
        - 3
        - 240
        - 240
      type: rgb
    robot0_eye_in_hand_image:
      shape:
        - 3
        - 240
        - 240
      type: rgb
    robot0_eef_pos:
      shape:
        - 3
      type: low_dim
    robot0_eef_quat:
      shape:
        - 4
      type: low_dim
    robot0_gripper_qpos:
      shape:
        - 2
      type: low_dim

num_envs: 4

# DP trains tool-hang at 240x240 with 216x216 random crop
crop_shape:
  - 216
  - 216
